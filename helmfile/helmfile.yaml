environments:
  dev:
  staging:
  dyna:
  main:
  prod:

releases:
  - name: ee-{{ requiredEnv "BRANCH" | lower }}
    chart: ../helm/charts/eligibility-estimator
    values:
      - overrides/eligibility-estimator/eligibility-estimator.yaml.gotmpl
    labels:
        category: dts
        app: {{ requiredEnv "PROJECT" }}
        tier: frontend
    namespace: eligibility-estimator
    installed: true
